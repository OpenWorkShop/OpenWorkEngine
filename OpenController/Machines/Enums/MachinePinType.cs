namespace OpenWorkEngine.OpenController.Machines.Enums {
  public enum MachinePinType {
    X, // X-axis limit pin.
    Y, // Y-axis limit pin.
    Z, // Z-axis limit pin.
    A, // A-axis limit pin.
    P, // Probe
    D, // Door
    H, // Hold
    R, // Soft-Reset
    S  // Cycle-Start
  }
}
