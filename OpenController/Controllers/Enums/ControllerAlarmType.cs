namespace OpenWorkEngine.OpenController.Controllers.Enums {
  public enum ControllerAlarmType {
    Unknown = 0,
    Limit,
    Abort,
    Probe,
    Homing,
    // HardLimit = 1,
    // SoftLimit = 2,
    // Abort = 3,
    // ProbeState = 4,
    // ProbeContact = 5,
    // HomingFailReset = 6,
    // HomingFailDoor = 7,
    // HomingFailClearLimitSwitch = 8,
    // HomingFailFindLimitSwitch = 9,
  }
}
