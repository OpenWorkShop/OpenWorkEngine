
// Main toolbar height for the app...
// 1) assigned as theme.mixins.toolbar.height (top bar of app)
// 2) ToolbarCard uses this
export const toolbarHeight = 50;

// Left-side drawer has 3 modes (hidden, small, large)
export const drawerWidthSm = 50;
export const drawerWidthLg = 240;

// Bar overtop the workspace visualizer w/ popover chips.
export const workBarHeight = 40;
